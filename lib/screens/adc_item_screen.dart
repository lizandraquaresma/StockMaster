import 'package:flutter/material.dart';

class AdcItem extends StatelessWidget {
  const AdcItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('adcItem'),
      ),
      body: const Center(
        child: Text('vamos adc'),
      ),

      
    );
  }
}


